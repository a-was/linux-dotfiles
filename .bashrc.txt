BGREEN='\[\033[1;32m\]'
DGREEN='\[\033[0;32m\]'
ORANGE='\[\033[1;31m\]'
RED='\[\033[0;31m\]'
BBLUE='\[\033[1;34m\]'
DBLUE='\[\033[0;34m\]'
NORMAL='\[\033[00m\]'

GREEN='\[\033[38;5;82m\]'
WHITE='\[\033[38;5;15m\]'
CYAN='\[\033[38;5;51m\]'
PURPLE='\[\033[38;5;165m\]'

# only host and path
# export PS1="${CYAN}\h ${WHITE}[${PURPLE}\w${WHITE}] \$ ${NORMAL}"

# with username@host and path
export PS1="${GREEN}\u${WHITE}@${CYAN}\h ${WHITE}[${PURPLE}\w${WHITE}] \$ ${NORMAL}"


# screenfetch

if [ -f $HOME/.bashrc_aliases ]; then
	. $HOME/.bashrc_aliases
else
	echo "Filed to load '.bashrc_aliases'"
fi

if [ -f $HOME/.bashrc_functions ]; then
	. $HOME/.bashrc_functions
else
	echo "Filed to load '.bashrc_functions'"
fi

set -o ignoreeof
set -o vi
# set -o noclobber
# l

export EDITOR='vi'
export VISUAL='vi'
echo "Welcome back ${USER} !"

